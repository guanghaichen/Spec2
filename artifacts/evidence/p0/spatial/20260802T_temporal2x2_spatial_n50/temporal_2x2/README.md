# Paired temporal-factor evidence

Minimum-prefix-discrepancy and maximum-concentration temporal arrangements are crossed with linear and critical authority growth on identical LIBERO initial states. The two arrangements have equal target-call budgets and identical gap multisets. See `manifest.json` for exact derivation, configuration, and hashes.
